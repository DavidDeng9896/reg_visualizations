{
  "name": "python-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Custom Code Python execution worker (port 8091)",
  "scripts": {
    "start": "node ./scripts/run-uvicorn.mjs",
    "dev": "node ./scripts/run-uvicorn.mjs --reload",
    "install-deps": "node ./scripts/install-deps.mjs",
    "test": "python -m pytest tests/ -v"
  }
}
