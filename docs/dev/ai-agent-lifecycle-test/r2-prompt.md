同一项目 hlx69，现在进入第 2 轮（r2）。继续用平台工具生成数据并分析，不要让我贴 CSV。

请基于第 1 轮湿实验（表达 + BLI）中表现较好的 topN 单点：

1. **组合突变**：将 topN 单点做 2–10 个突变的排列组合，再做一轮算法打分（本轮提高 flexddg 权重；FoldX 在组合突变中一般不用）。选出 topN 或 Vote 组合。
2. 也可包含：用湿实验结果对模型做一次模拟微调后的单点重打分，或第一轮次 topM 单点。
3. 命名：`hlx69-r2-aicmxx`（组合）/ 如有单点则 `hlx69-r2-aismxx`，排序规则同文档（先重链后轻链 → 位点编号 → 突变后 A-Z）。
4. 生成 r2 百英表达 + 通用 BLI（可含重复；均值；>3 倍差异提醒）。
5. 与 WT、r1 最优 Candidate 对比 Kd 与 fold improvement；判断是否达到 10 倍提升、能否交付。
6. 更新图表/看板，给出中文结论（是否达标、推荐交付序列、若未达标下一步建议）。

组合打分表不强制过千行，但必须落表；继续用 Custom Code 生成组合，不要粘贴大 CSV。
